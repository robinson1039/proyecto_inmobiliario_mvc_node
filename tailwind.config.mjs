/** @type {import('tailwindcss').Config} */
export default {
  content: ['./views/**/*.pug'], // se configura para que analice todos los archivos .pug de la carpeta y aplique los estilos ene sa carpeta para eso se usan los **
  theme: {
    extend: {},
  },
  plugins: [],
}

